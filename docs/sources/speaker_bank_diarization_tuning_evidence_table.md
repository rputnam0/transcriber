## Evidence Table

| claim_id | citation_id | paper_id | location | claim_type | equation_as_published | symbol_units | validity_domain | calibration_requirements | failure_modes | implementation_note | confidence_tier | ocr_quality | reconciliation_note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C-001 | CIT-001 | snyder-2018-xvectors | section:2.2 | algorithm | `x-vectors are extracted from variable-length speech segments and scored with an LDA/PLDA backend` | `x: speaker embedding (unitless)` | Speaker recognition with labeled training speakers | Requires speaker-labeled utterances; no diarization labels needed | Backend mismatch can dominate if enrollment/test channels differ | Use the repo's existing speaker bank as the closed-set enrollment store and improve the backend before retraining embeddings. | A | not_ocr | Repo uses cosine/prototype scoring rather than PLDA, so backend calibration is the adaptation layer. |
| C-002 | CIT-002 | matejka-2017-asnorm | section:3;eq:7 | equation | `0.5 * (((s - mu_e) / sigma_e) + ((s - mu_t) / sigma_t))` | `s: raw similarity score; mu/sigma: cohort score statistics (unitless)` | Speaker verification under language/channel mismatch | Requires a cohort of impostor embeddings and a chosen top-K cohort size | Poor cohort choice can destabilize scores | Implement adaptive symmetric score normalization over the bank's non-target speaker embeddings. | A | not_ocr | Normative equation for AS-Norm in this repo. |
| C-003 | CIT-006 | perez-2024-normalization | section:Results | validation | `normalization applied at the embedding or score level increases verification performance` | `normalized embeddings/scores (unitless)` | Realistic, mismatched speaker-recognition conditions | Needs held-out evaluation under realistic mismatch | Overfitting the backend to a single session can hide channel shift | Keep whitening optional but available, and validate it in the Session 22 word-level harness. | B | not_ocr | Used as justification for whitening in addition to AS-Norm. |
| C-004 | CIT-003 | speechbrain-speaker-cls | section:Overview | algorithm | `EncoderClassifier.classify_batch(...)` and fine-tuning from labeled speech | `waveform: audio tensor; class: speaker label` | Closed-set speaker classification | Requires utterance-level speaker labels and train/valid/test splits | A classifier trained on one mic/domain can overfit quickly | Fine-tuning a speaker-ID model is feasible, but should follow backend calibration so we know the remaining error is model-limited. | B | not_ocr | Secondary official implementation evidence, not the normative math source. |
| C-005 | CIT-004 | pyannote-audio-readme | section:Transfer learning | parameter | `~10h of manually annotated dataset can be sufficient for transfer learning` | `annotation hours: h` | Diarization / speaker-embedding fine-tuning on in-domain data | Requires manually annotated diarization or speaker labels | Fine-tuning is expensive and can regress if labels are noisy | Treat pyannote fine-tuning as phase 2 after score-backend tuning, not the first fix. | B | not_ocr | Supports feasibility of fine-tuning with the existing labeled archive. |
| C-006 | CIT-005 | pyannote-community-1 | section:Model card | limitation | `better speaker assignment/counting` for diarization does not eliminate downstream closed-set ID needs | `speaker labels/timestamps` | Multi-speaker conversational diarization | Requires accepted model terms and diarization inference support | Diarization improvements alone do not identify known cast members | Keep diarization and speaker-bank scoring as separate evaluation axes in the harness. | B | not_ocr | Supports not conflating diarization quality with closed-set speaker assignment quality. |
