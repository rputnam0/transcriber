# Speaker Isolation Literature Evidence Table

| claim_id | citation_id | source_id | location | claim_type | equation_or_rule_as_published | symbol_units | validity_domain | calibration_requirements | failure_modes | implementation_note | confidence_tier | ocr_quality | reconciliation_note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C-001 | CIT-001 | wang-2019-voicefilter | arXiv abstract lines 37-39 | algorithm | VoiceFilter separates a target speaker from multi-speaker signals using a reference signal, a speaker-recognition network that produces speaker-discriminative embeddings, and a spectrogram masking network conditioned on noisy spectrogram plus speaker embedding. | `x_ref`: reference speech waveform; `e_spk`: unitless speaker embedding; `M`: spectrogram mask | Single-channel multi-speaker mixtures where a reference utterance exists for the target speaker | Needs target-speaker enrollment audio and paired or simulated mixtures for training/fine-tuning | Mask can distort single-speaker speech; performance depends on reference/mixture domain match | Use speaker-bank enrollments as the conditioning source; evaluate enhanced waveform by downstream word-speaker accuracy, not SDR alone. | A | not_ocr | Aligns with repo oracle-mask result but replaces oracle stem mask with a learned speaker-conditioned mask. |
| C-002 | CIT-002 | zmolikova-2019-speakerbeam | page:0 lines 14-18; page:3 lines 261-280 | equation | SpeakerBeam extracts the target speaker using an adaptation utterance and estimates a mask: `M = g(|Y|, |A|)` and `S_hat_0 = M * Y`. The authors state target extraction avoids label permutation, dependence on number of speakers, and speaker tracing. | `Y`: mixture STFT; `A`: adaptation utterance STFT; `M`: time-frequency mask; `S_hat_0`: estimated target STFT | Single-channel or multi-channel target speaker extraction from mixtures with adaptation speech | Needs adaptation utterance per target and training mixtures with target/interferer references | Simple d-vector concatenation may fail when target/interferer energy conditions differ; adaptation length and domain matter | Prefer conditioning designs richer than concatenating an embedding to a shallow network; include energy-buried targets in training. | A | clear | Directly addresses the source-permutation bug class seen in repo blind-separation experiments. |
| C-003 | CIT-003 | ge-2020-spexplus | arXiv abstract lines 42-44 | algorithm | SpEx+ performs complete time-domain speaker extraction from a multi-talker environment using target reference speech and tied speech encoders for extraction and speaker encoding. | `x_mix`: mixture waveform; `x_ref`: target reference waveform; outputs target waveform | Time-domain target speaker extraction on two-speaker extraction benchmarks | Requires clean target references and target/interferer supervised mixtures | Benchmark performance may not transfer to Discord/meeting domain without fine-tuning | Treat SpEx+/time-domain extractor as a stronger stage-2 model family if TS-VAD does not recover enough word attribution accuracy. | A | not_ocr | Same target-conditioned family as VoiceFilter/SpeakerBeam, but closer to ConvTasNet-style waveform separation. |
| C-004 | CIT-004 | medennikov-2020-tsvad | arXiv abstract lines 37-39 | algorithm | TS-VAD directly predicts the activity of each speaker at each time frame, taking conventional speech features plus i-vectors for each speaker and producing binary activity outputs per speaker. | `X_t`: acoustic features at frame `t`; `v_s`: speaker vector for speaker `s`; `p(active_s,t)`: activity probability | Known-speaker or initialized-speaker diarization, especially overlapped speech scenarios | Needs frame-level speaker activity labels or diarization labels and speaker vectors; post-processing thresholds must be calibrated | Requires known/estimated speaker profiles and can inherit errors from bad initial speaker vectors | Build a closed-set target-speaker activity model over the repo's known party speakers, using multitrack stems to create frame labels. | A | not_ocr | Strongest fit to the repo metric because it predicts who is active without requiring a separated waveform. |
| C-005 | CIT-005 | ding-2020-personalvad | arXiv abstract lines 38-39 | algorithm | Personal VAD is a VAD-like neural network conditioned on a target speaker embedding or speaker verification score; each frame outputs non-speech, target-speaker speech, or non-target-speaker speech probabilities. | `e_target`: target embedding; `p(non_speech,target,non_target)_t`: frame class probabilities | Streaming/on-device speaker-conditioned VAD; target user known in advance | Needs target enrollment and target/non-target speech labels; can be trained compactly | Three-class PVAD does not directly identify multiple simultaneous named speakers unless run per speaker or extended to multi-label | Use as a minimal one-vs-rest architecture: run or batch one target-conditioned detector per known speaker and combine probabilities at word time. | A | not_ocr | Complements TS-VAD when a simpler target-conditioned classifier is easier than a full multi-speaker model. |
| C-006 | CIT-006 | luo-2019-convtasnet | arXiv abstract lines 42-46 | algorithm | Conv-TasNet uses a learned waveform encoder, separator masks, and decoder for single-channel speaker-independent time-domain separation; masks are estimated by a temporal convolutional network. | `x_mix`: waveform; encoder features; `M_i`: source masks; `x_hat_i`: separated waveforms | Speaker-independent two- and three-speaker mixture separation | Requires supervised mixture/source pairs and PIT/source assignment during training | Does not identify which separated stream belongs to which known speaker; domain mismatch can break downstream speaker ID | Keep ConvTasNet as a separable component only if trained/evaluated with downstream word-speaker labels and source assignment. | A | not_ocr | Repo results show small ConvTasNet variants do not beat mixed audio; literature explains why source assignment remains unsolved. |
| C-007 | CIT-007 | subakan-2021-sepformer | arXiv abstract lines 42-44 | validation | SepFormer is a Transformer-based speech separation model that learns short- and long-term dependencies and reports strong WSJ0-2/3mix SI-SNR improvements. | `SI-SNRi`: dB improvement; waveform inputs/outputs | Standard simulated speech separation benchmarks | Needs enough compute/data for high-capacity training or fine-tuning | Benchmark gains do not guarantee speaker-label gains after separation on continuous Discord audio | Use high-capacity separators only after defining a target-conditioned or downstream-speaker-ID objective. | A | not_ocr | Supports model-family choice but not as a drop-in solution. |
| C-008 | CIT-008 | wang-2023-tfgridnet | arXiv abstract lines 38-42 | validation | TF-GridNet integrates full-band, sub-band, and cross-frame self-attention modules for complex spectral mapping and reports state-of-the-art separation results across anechoic, reverberant, and noisy-reverberant tasks. | `RI`: real/imaginary spectral components; `SI-SDR`: dB | Monaural and multi-microphone speech separation benchmarks | Requires STFT complex targets and larger training budget | May optimize signal metrics without improving word speaker attribution if source identity is unresolved | Candidate architecture for a serious stage-2 target extraction experiment, not the first MVP. | A | not_ocr | Strong architecture evidence, but adaptation to closed-set speaker identity still required. |
| C-009 | CIT-009 | chen-2020-libricss | page:0 lines 5-24; page:0 lines 29-32; page:0 lines 64-85 | limitation | Continuous speech separation generates non-overlapped signals from continuous audio with partially overlapping utterances. The paper warns that pre-segmented fully-overlapped tests and signal metrics such as SDR/SISNR can be weakly correlated with ASR accuracy and practical relevance. | Continuous audio stream; overlap ratio; ASR accuracy; SDR/SISNR | Meeting-style continuous audio with partial overlaps and overlap-free regions | Must evaluate on continuous windows and downstream ASR/diarization outcomes | Separators may distort single-speaker regions; good SDR can fail downstream ASR/diarization | Keep using word-speaker accuracy and same-row mixed baselines; include overlap-free distortion checks before integration. | A | clear | Justifies why repo experiments score separated audio through the actual speaker-ID pipeline. |
| C-010 | CIT-010 | maiti-2022-eendss | arXiv abstract lines 37-40 | algorithm | EEND-SS jointly performs speaker diarization, speech separation, and speaker counting by combining EEND, encoder-decoder attractors, Conv-TasNet masks, and diarization-informed separation refinement. | Frame speaker activity; speaker count; separated waveform masks | Flexible-number speaker diarization/separation benchmarks | Needs diarization labels plus source separation supervision | Larger training and integration surface; may be overkill for closed-set known speakers | Use as a later blueprint if TS-VAD plus target extraction needs joint training. | A | not_ocr | Supports joint modeling but not the immediate minimal implementation. |
| C-011 | CIT-011 | kanda-2021-saasr | arXiv abstract lines 37-40 | algorithm | Speaker-attributed ASR with Transformer jointly performs speaker counting, speech recognition, and speaker identification for monaural multi-talker audio, with reported LibriCSS results using target speaker profiles. | Words, speaker identities, speaker count; cpWER | Monaural multi-talker ASR, including LibriCSS | Requires ASR model training/fine-tuning and target speaker profile integration | High implementation cost; model must replace or deeply wrap current ASR | Treat as a long-term route if modular diarization cannot reach desired accuracy. | A | not_ocr | Useful because the final metric is word-level speaker attribution, but too invasive as next step. |
| C-012 | CIT-012 | kanda-2022-transcribe-to-diarize | arXiv abstract lines 37-41 | algorithm | Transcribe-to-Diarize uses end-to-end speaker-attributed ASR internals to estimate word times and speaker activity, leveraging linguistic information and simultaneously generating speaker-attributed transcription. | Word start/end times; speaker activity; speaker-attributed transcript | Diarization and transcription on LibriCSS/AMI-style data | Requires a speaker-attributed ASR model and enough task-matched training data | Hard to integrate with the current faster-whisper/parakeet pipeline; less modular | Keep as a research compass for a future ASR-integrated architecture. | A | not_ocr | Confirms that the solved version of this problem often fuses speech recognition and speaker attribution rather than post-hoc embeddings only. |
| C-013 | CIT-013 | transcriber-ledger | docs/speaker-id-experiment-ledger.md | validation | Repo experiments show clean-source/oracle-isolated speech can reach about 90-96% word speaker accuracy, while hard mixed-overlap words remain far lower. A larger/deeper true-target TasNet now reaches 73.07% leave-window and 64.13% strict-session hard accuracy, but all-candidate deployable selection still tops out at 57.64% while the oracle mixed-plus-any-candidate score reaches 84.44%, temporal decoding over candidate scores reaches only 57.04%, and full-context mixed temporal decoding reaches only 57.50% on the hard slice. Larger/deeper word-owner silence calibration improves the 300-row oracle space to 84.33% but still only reaches 60.67% deployably versus 59.67% mixed; adding an explicit owner head reaches only 60.33% deployably. Stronger all-source fixed speaker channels improve true-channel accuracy to 69.33% and oracle mixed-plus-any-candidate accuracy to 85.00%, but still tie mixed deployably at 59.67% and score only 30.33% by owner argmax. Activity-confidence fusion, aggregate class evidence across candidate outputs, ReLU/softplus mask activation swaps, and full-window sequence candidate routers also fail to beat the current baselines. Small/generic separators, shallow selectors, temporal smoothing over current candidate or mixed scores, word-owner silence/owner-head/fixed-channel calibration, matched STFT U-Nets, simple word-local source energy, lightweight WavLM crop fine-tuning, pause-bounded embedding aggregation, and shallow enrollment encoders do not justify integration. | Word speaker accuracy; target energy share; selected word rows | This repo's Discord/multitrack evaluation harness | Must preserve split hygiene and compare same rows | Local experiments are not literature; they define repo-specific fit and kill criteria | Use literature methods only if they beat the repo's same-row mixed baselines, produce a deployably selectable candidate space, and move toward the oracle-mask ceiling. | A | not_ocr | Bridges external evidence to the local objective and rejects drop-in separator, post-hoc-selector, shallow objective-tweak, phrase-smoothing, sequence smoothing over weak candidate/mixed emissions, silence-only calibration, simple owner heads, fixed-channel output identity/owner weighting, activity-confidence routing, aggregate candidate-class evidence, mask-activation swaps, sequence candidate routers, or pooled crop-classifier optimism while promoting high-capacity calibrated target extraction. |
| C-014 | CIT-014 | yang-2024-contextual-tse | arXiv abstract lines 37-40 | algorithm | Contextual TSE computes attention between the enrollment and mixed-signal time-frequency representations, using the resulting consistent enrollment representations as guidance rather than relying only on a precomputed speaker embedding. | Enrollment T-F features; mixed T-F features; attention weights; target guidance | Target-speaker extraction with enrollment audio, including complex scenarios | Needs enrollment audio/features and paired mixture/target supervision | Benchmark gains may not transfer unless trained on Discord-style mixtures and evaluated by word speaker accuracy | Test direct enrollment/mix cross-attention before more centroid-conditioned TasNet variants. | A | not_ocr | Matches local evidence that clean-bank centroids alone are weak conditioning for extraction. |
| C-015 | CIT-015 | zeng-2024-usef-tse | arXiv abstract lines 37-41 | algorithm | USEF-TSE removes the dependency on an external speaker-recognition embedding and uses a multi-head cross-attention mechanism as a frame-level target-speaker feature extractor that can integrate with time-domain or T-F separation models. | Enrollment speech frames; target-speaker frame features; separated waveform or mask | Monaural target-speaker extraction on anechoic, noisy, reverberant, and out-of-domain benchmarks | Needs target-speaker enrollment and supervised extraction data | Still optimizes signal metrics and may need domain adaptation to preserve word-speaker labels | Prefer embedding-free or direct-enrollment conditioning over another frozen-speaker-vector swap. | A | not_ocr | Directly addresses the repo's failed frozen backend and centroid-conditioning paths. |
| C-016 | CIT-016 | zhang-2024-multilevel-tse | arXiv abstract lines 41-44 | validation | Multi-level TSE combines raw enrollment magnitude-spectrogram features with contextual embeddings from a speaker encoder, reporting a 2.74 dB SI-SDR improvement and 4.94% extraction-accuracy increase over baseline on Libri2Mix. | Enrollment magnitude spectrogram; frame-level speaker embeddings; extraction accuracy | Libri2Mix target-speaker extraction | Needs multi-level enrollment feature extraction and target/interferer supervision | Libri2Mix gains may overstate Discord meeting gains; still must validate by word speaker accuracy | If building a new extractor, include low-level enrollment spectral features in addition to identity embeddings. | A | not_ocr | Offers a concrete design reason why fixed Titanet/ECAPA centroids may discard useful extraction cues. |
| C-017 | CIT-017 | navon-2025-flowtse | arXiv abstract lines 38-40 | algorithm | FlowTSE uses conditional flow matching with enrollment and mixed speech represented as mel-spectrograms, and proposes a vocoder conditioned on the mixture complex STFT for phase-sensitive reconstruction. | Enrollment mel; mixed mel; generated target mel/waveform; mixture complex STFT | TSE benchmarks where generative extraction can be trained | Requires larger generative training/inference budget and paired clean targets | Phase/vocoder quality and latency may complicate integration; benchmark quality does not guarantee speaker-label gain | Treat flow-matching TSE as a stronger but heavier stage-2 backbone if cross-attention TasNet-style extraction plateaus. | A | not_ocr | Reinforces that the next upper-90 attempt may need a stronger backbone, not another shallow mask. |
| C-018 | CIT-018 | xu-2025-posneg-enrollment | arXiv abstract lines 37-40; PDF pages 1-3 | algorithm | Positive/negative enrollment TSE encodes the target speaker by comparing noisy segments where the target is speaking with segments where the target is silent; the paper reports over 2.1 dB higher SI-SNRi than prior work and faster convergence. | Positive enrollment audio; negative enrollment audio; target-speaker characteristic; target waveform | Noisy conversational settings where clean target enrollment is unavailable or contaminated | Needs positive and negative segment labels for target presence; can use imperfect labels | Assumes target/interferer speaking patterns differ enough for comparison; artifacts remain possible | This repo has labeled per-speaker timelines, so positive/negative segment conditioning is a promising domain-specific TSE design. | A | clear | Particularly relevant because Discord clean labels can generate target-present and target-absent enrollment examples cheaply. |
| C-019 | CIT-019 | pallala-2025-targetvoice | ISCA page lines 4-8 | implementation | TargetVoice is a compact low-latency TSE model that extracts the enrolled speaker from multi-speaker/noisy environments, using a 3 second enrollment with a compact speaker encoder and extraction block, and is reported to improve ASR in real-world conditions. | 3 s enrollment; speaker embedding; low-latency extraction block; ASR accuracy | Real-world calls, conference calls, hands-free communication, and smart-speaker settings | Needs access to model details or reimplementation; commercial/in-house data strategy may not be public | Short paper page does not provide enough reproducible training detail | Use as evidence that compact deployed TSE is plausible, but not as a direct implementation recipe. | A | not_ocr | Supports the user's intuition that this is solved in adjacent call/meeting contexts, while still requiring repo-specific training. |
